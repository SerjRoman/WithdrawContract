import * as styles from './{{name}}.module.css';

interface {{toUpperCase name}}Props {
    
}

export default function {{toUpperCase name}}(props: {{toUpperCase name}}Props) {
    const {

    } = props
    return (
        <{{tag}} className={ styles.{{toUpperCase name}} }>

        </{{tag}}>
    )
}
